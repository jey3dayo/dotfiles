# Terraform CLI config。XDG 配下を読む機能は Terraform に無いため、
# mise dotfiles で ~/.terraformrc へ symlink して配布する。

# provider を作業ディレクトリごとに複製せず共有する。
# 未設定だと 1 ディレクトリあたり AWS provider だけで約 780MB を個別に抱える。
# パス展開は $HOME のみ保証されるため XDG 変数は使わない。
plugin_cache_dir = "$HOME/.cache/terraform/plugin-cache"
