_dotfiles_ni_completion="${XDG_DATA_HOME:-$HOME/.local/share}/sheldon/repos/github.com/snowsman/ni-completion/.ni.zsh"
[[ -r "$_dotfiles_ni_completion" ]] && source "$_dotfiles_ni_completion"
unset _dotfiles_ni_completion

# vim: set syntax=zsh:
