# Implementation Plans

Generated by the improve skill on 2026-07-14; extended 2026-08-14.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done — unless a reviewer maintains the index.

## Branch map (2026-08-14 round)

| Branch                          | Plans    | Rationale                                          |
| ------------------------------- | -------- | -------------------------------------------------- |
| `advisor/secret-path-hardening` | 005, 006 | Script/security changes (env modes + WSL snapshot) |
| `advisor/docs-stale-cleanup`    | 007      | Docs/comments only — different blast radius        |

Do not mix the two branches. Do not push/PR unless the operator asks.

## Execution order & status

| Plan | Title                                                 | Priority | Effort | Depends on | Status                         |
| ---- | ----------------------------------------------------- | -------- | ------ | ---------- | ------------------------------ |
| 001  | Pre-push hook uses fast lint                          | P1       | S      | —          | DONE                           |
| 002  | Add test coverage for env-detect.sh                   | P1       | S      | —          | DONE                           |
| 003  | Align go/node version pinning across mise configs     | P2       | S      | —          | DONE                           |
| 004  | Fix stale/broken documentation references             | P2       | S      | —          | DONE                           |
| 005  | Create `.env.local` with mode 0600 from the start     | P1       | S      | —          | DONE (worktree; pending merge) |
| 006  | Harden WSL mise snapshots (env exclude + allowlist)   | P1       | S      | —          | DONE (worktree; pending merge) |
| 007  | Align docs with post-mise reality (pre-push + Nix/HM) | P1       | S      | —          | DONE (worktree; pending merge) |

## Dependency notes

- 005 and 006 are independent but share `advisor/secret-path-hardening`.
- 007 is independent on `advisor/docs-stale-cleanup`.
- Finding #1 (setup-env modes) was briefly considered a local compromise;
  maintainer confirmed mode 0600 is desired — planned as 005.

## Findings considered and rejected

- `aicommits-with-env` brief world-readable key window — fixed earlier
  (`chmod 700` on tmp home before writing key); not re-planned.
- GHA `$HOME/.config` full copy — by-design (`fd` skips symlinks); not planned.
- README `最終更新` date-only bump — low leverage alone; optional in 007.

## Findings audited but not yet planned

- `bootstrap.sh` characterization tests (M)
- `mise/lib` gate helper tests (M)
- `bin/` TS discovery + GHA path filters (S)
- `npm:husky` removal (S)
- `TASK_EXCLUDES` / `MD_EXCLUDES` dedupe (S)
- `mise run setup` raw pip pynvim (S)
- `format.toml` unquoted `${*_FILES}` (S)
- go/node/eza pin decisions left open by plan 003 (S, needs human)
- mise `[tools]` OS overlay DRY (M)
- pre-push whole-repo format double-work (M)
- Direction: WSL2 cutover (`todo.txt`), luarocks/lua FIXME, `+nix-removal`
