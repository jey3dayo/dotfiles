# Implementation Plans

Generated by the improve skill on 2026-07-14. Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                             | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Pre-push hook uses fast lint                      | P1       | S      | —          | DONE   |
| 002  | Add test coverage for env-detect.sh               | P1       | S      | —          | DONE   |
| 003  | Align go/node version pinning across mise configs | P2       | S      | —          | DONE   |
| 004  | Fix stale/broken documentation references         | P2       | S      | —          | DONE   |

## Dependency notes

None — all four plans touch disjoint files and can run fully in parallel.

## Findings considered and rejected

- (none yet — this is the initial plan set from the 2026-07-14 standard audit)

## Findings audited but not yet planned

These survived vetting but were not selected for this round. Re-audit only
if the underlying code changes significantly:

- `bootstrap.sh` (298 lines) has no test coverage — higher effort (M) than
  the four selected plans; a good next candidate.
- `mise/lib/*.sh` task-runner glue scripts untested.
- mise config `[tools]` layering duplicated across 5 files with a
  repeated stale FIXME comment (lua/luarocks) — tech-debt, effort M, tied to
  the still-open WSL2 Home-Manager migration.
- `aicommits-with-env:54-55` — plaintext API key briefly world-readable
  before `chmod 600`; low severity (single-user machine), effort S.
- `scripts/windows/mise-wsl-task.{sh,ps1}` clone working tree to `/tmp`
  without excluding `.env*`; effort S.
- `mise/local-tasks/format.toml` — several `${VAR}` file-list expansions
  unquoted; effort S, low risk.
- `mise-wsl-task.ps1`'s default-branch task name interpolation has
  unvalidated escaping (no current exploit path; add input validation).
- Direction: finish the stalled WSL2 → mise-bootstrap migration (tracked in
  `TODO.md`, blocked on physical/remote access to the WSL2 machine).
- Direction: resolve the luarocks 3.13.0 rockspec parsing issue blocking
  native `lua`/`luacheck` provisioning (currently supplied via legacy Nix on
  Pi) — this is also the last blocker on removing the Nix runtime entirely.
