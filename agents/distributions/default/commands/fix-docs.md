---
description: Comprehensive documentation fix system with link validation
argument-hint: [path] [--config-only] [--links-only] [--format-only] [--optimize-structure] [--dry-run]
---

# Fix Documentation - 包括的ドキュメント修正システム

プロジェクトの全ドキュメントを自動解析し、リンク修正、フォーマット最適化、構造改善を実行する次世代コマンドです。

## 🚀 主要機能

### インテリジェント検出

- **リンク検証**: 内部・外部リンクの生死確認と自動修正
- **フォーマット最適化**: Markdown構文エラーの自動検出・修正
- **構造分析**: ドキュメント階層の最適化提案
- **重複検出**: 重複コンテンツや矛盾の発見

### docs-manager統合

内部的にdocs-managerエージェントを活用し、包括的なドキュメント品質管理を実現します。

## 📋 実行フェーズ

### Phase 1: プロジェクト分析と設定検出

```python
def analyze_documentation_structure():
    """ドキュメント構造と設定を分析"""

    # 1. プロジェクト構造の検出
    project_info = detect_project_type()

    # 2. ドキュメントファイルの収集
    doc_files = glob_markdown_files([
        "README.md", "CHANGELOG.md", "CONTRIBUTING.md",
        "docs/**/*.md", ".claude/**/*.md"
    ])

    # 3. 設定ファイルの検出
    config_files = detect_config_files([
        ".markdownlint-cli2.jsonc",
        ".markdownlint.yml",
        ".markdownlintrc",
        ".markdown-link-check.json"
    ])

    # 4. タスクランナーの検出
    task_runners = detect_task_runners({
        "mise": check_mise_tasks("docs:*"),
        "pnpm": check_package_scripts("docs:*"),
        "npm": check_package_scripts("docs:*")
    })

    # 5. 実行戦略の決定
    execution_strategy = determine_execution_strategy(
        config_files, task_runners
    )

    return {
        "files": doc_files,
        "project": project_info,
        "config": config_files,
        "task_runners": task_runners,
        "strategy": execution_strategy
    }
```

**検出される設定ファイル:**

- `.markdownlint-cli2.jsonc` - Markdownlint設定（JSON形式、優先）
- `.markdownlint.yml` - Markdownlint設定（YAML形式）
- `.markdown-link-check.json` - リンクチェック設定

**検出されるタスクランナー:**

- `mise.toml` の `docs:*` タスク（`docs:fix`, `docs:lint`, `docs:links`, `docs:check`）
- `package.json` の `docs:*` スクリプト

### Phase 2: 実行戦略の選択

Phase 1 の分析結果に基づいて、最適な実行方法を選択します。

#### 優先順位

##### 1. mise タスク（最優先）

`mise.toml` に `docs:*` タスクが定義されている場合、これを優先的に使用：

```bash
# 自動修正
mise run docs:fix

# Lint検証
mise run docs:lint

# リンクチェック
mise run docs:links

# 包括的チェック
mise run docs:check
```

##### 2. pnpm/npm スクリプト

`package.json` に `docs:*` スクリプトが定義されている場合：

```bash
pnpm docs:fix
npm run docs:fix
```

##### 3. 直接実行（タスク定義がない場合）

設定ファイルを自動検出して実行：

```bash
# Markdownlint自動修正（設定ファイルを自動検出）
pnpx markdownlint-cli2 --fix '**/*.md'

# リンクチェック（設定ファイルを明示的に指定）
fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json
```

##### 4. docs-manager エージェント（高度な分析が必要な場合）

以下の場合にエージェントを起動：

- 構造最適化が必要
- メタデータ検証が必要
- プロジェクト固有ルールの適用
- 重複・矛盾コンテンツの統合

```python
def determine_execution_method(context):
    """実行方法を決定"""

    # mise優先
    if context["task_runners"]["mise"]["docs:fix"]:
        return "mise run docs:fix"

    # pnpm/npm次点
    if context["task_runners"]["pnpm"]["docs:fix"]:
        return "pnpm docs:fix"

    # 設定ファイル活用
    if context["config"]["markdownlint"]:
        return "pnpx markdownlint-cli2 --fix '**/*.md'"

    # エージェント起動
    return "docs-manager agent"
```

### Phase 3: 問題検出と分類

各ドキュメントを多角的に分析し、修正が必要な問題を特定：

#### リンク問題

- 内部リンク切れ: `[text](./missing-file.md)`
- 外部リンク切れ: `[text](https://dead-link.com)`
- 相対パス問題: `../../../complex-path.md`

#### フォーマット問題

- Markdownシンタックスエラー
- 不適切なヘッダー階層
- テーブルフォーマット不整合
- コードブロック言語指定不足

#### 構造問題

- 目次（TOC）の不整合
- セクション順序の論理性
- 重複・矛盾するコンテンツ

### Phase 4: 設定ファイル最適化

#### .markdownlint.yml 作成・最適化

プロジェクトに`.markdownlint.yml`が存在しない場合、以下を自動提案・作成：

```yaml
# Markdownlint configuration for this project
extends: default

rules:
  # Line length - プロジェクトに応じて調整
  MD013:
    line_length: 120
    code_blocks: false
    tables: false

  # Inline HTML - プロジェクトで必要な場合は許可
  MD033: false

  # Multiple headings - README等で必要
  MD025:
    front_matter_title: "^\\s*title\\s*[:=]"

  # Heading punctuation
  MD026:
    punctuation: ".,;:!。，；：！"

  # Trailing spaces
  MD009:
    br_spaces: 2

  # Hard tabs
  MD010: false # プロジェクトのタブポリシーに従う
```

**プロジェクト別カスタマイズ例:**

- **API ドキュメント**: コードブロックの言語指定を厳格化
- **チュートリアル**: 長い行を許可、インライン HTML を有効化
- **技術仕様書**: 厳格なヘッダー階層、テーブル整列を強制

### Phase 5: 自動修正実行

各問題カテゴリに対して最適な修正手法を適用：

```python
def execute_automated_fixes(detected_issues):
    """自動修正の実行"""

    fixes_applied = []

    # 1. リンク修正 (高優先度)
    for link_issue in detected_issues["broken_links"]:
        if fix_result := attempt_link_repair(link_issue):
            fixes_applied.append(fix_result)

    # 2. フォーマット修正
    for file in detected_issues["format_issues"]:
        # prettier適用
        prettier_result = apply_prettier_formatting(file)

        # markdownlint修正
        lint_result = apply_markdownlint_fixes(file)

        fixes_applied.extend([prettier_result, lint_result])

    # 3. 構造最適化
    for structure_issue in detected_issues["structure_problems"]:
        fix_result = optimize_document_structure(structure_issue)
        fixes_applied.append(fix_result)

    return fixes_applied
```

## 🔧 ツール統合

### Markdownlint

- **自動設定**: `.markdownlint.yml` の生成と最適化
- **ルールカスタマイズ**: プロジェクト特性に応じたルール調整
- **修正提案**: 検出された問題の自動修正

### Prettier

- **統一フォーマット**: Markdownファイルの一貫したスタイル適用
- **設定連携**: `.prettierrc` との統合
- **エディタ設定**: VSCode/WebStorm設定の最適化提案

### mise/その他ツール

- **mise run lint**: プロジェクト固有のリンティング実行
- **カスタムスクリプト**: package.json の docs関連スクリプト活用

## 🔧 設定ファイル統合

### Markdownlint設定

**自動検出される設定ファイル:**

- `.markdownlint-cli2.jsonc` (優先、JSON形式)
- `.markdownlint.yml` (YAML形式)
- `.markdownlintrc` (レガシー形式)

`markdownlint-cli2` は設定ファイルを自動検出します。明示的に指定する必要はありません。

**設定例（`.markdownlint-cli2.jsonc`）:**

```jsonc
{
  "config": {
    "default": true,
    "MD013": false, // 行長制限を無効化
    "MD024": false, // 重複見出しを許可
    "MD033": {
      // インラインHTML許可
      "allowed_elements": ["details", "summary"],
    },
  },
  "ignores": [
    "**/node_modules/**",
    "**/dist/**",
    "**/_*.md", // アンダースコアで始まるファイルを除外
  ],
}
```

**実行コマンド:**

```bash
# 設定ファイルを自動検出
pnpx markdownlint-cli2 '**/*.md'

# 自動修正
pnpx markdownlint-cli2 --fix '**/*.md'
```

### リンクチェック設定

**設定ファイル:** `.markdown-link-check.json`

markdown-link-check では設定ファイルを明示的に指定することを推奨します。

**設定例（`.markdown-link-check.json`）:**

```json
{
  "ignorePatterns": [
    { "pattern": "^http://localhost" },
    { "pattern": "^https://localhost" },
    { "pattern": "^#" },
    { "pattern": "^mailto:" }
  ],
  "replacementPatterns": [{ "pattern": "^/", "replacement": "file://." }],
  "httpHeaders": [
    {
      "urls": ["https://docs.github.com", "https://github.com"],
      "headers": {
        "Accept": "text/html,application/xhtml+xml"
      }
    }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 2,
  "aliveStatusCodes": [200, 206, 301, 302, 303]
}
```

**実行コマンド:**

```bash
# 設定ファイルを明示的に指定
fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json --quiet

# 詳細出力
fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json --verbose
```

### 設定ファイルがない場合

プロジェクトに設定ファイルがない場合、以下を提案：

#### 1. 基本設定の生成

`.markdownlint-cli2.jsonc` と `.markdown-link-check.json` のテンプレートを提供

#### 2. プロジェクト特性に応じたカスタマイズ

- **API ドキュメント**: コードブロック言語指定を厳格化（`MD040: true`）
- **チュートリアル**: 長い行を許可（`MD013: false`）、インラインHTMLを有効化
- **技術仕様書**: 厳格なヘッダー階層、テーブル整列を強制

#### 3. docs-manager エージェントによる生成

高度な分析に基づいて最適な設定を生成

## 🔄 mise タスク統合

### mise.toml での推奨タスク定義

```toml
# === ドキュメント ===
[tasks."docs:lint"]
description = "Markdownファイルのlint"
run = "pnpx markdownlint-cli2 '**/*.md'"

[tasks."docs:links"]
description = "Markdownリンクチェック"
run = "fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json --quiet"

[tasks."docs:links:verbose"]
description = "Markdownリンクチェック（詳細）"
run = "fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json --verbose"

[tasks."docs:check"]
description = "全Markdownドキュメントのlintとリンクチェック"
depends = ["docs:lint", "docs:links"]

[tasks."docs:fix"]
description = "Markdownドキュメントの自動修正"
run = "pnpx markdownlint-cli2 --fix '**/*.md'"
```

### /fix-docs での自動統合

`/fix-docs` コマンド実行時の動作：

```python
def execute_fix_docs():
    """fix-docs コマンドの実行フロー"""

    # 1. mise.toml の存在確認
    if has_mise_toml():
        # 2. docs:fix タスクの確認
        if has_task("docs:fix"):
            # mise タスクを優先実行
            run("mise run docs:fix")
            run("mise run docs:links")
            return

    # 3. 設定ファイルの確認
    config_files = detect_config_files()

    # 4. 直接実行または設定生成
    if config_files["markdownlint"]:
        # Markdownlint実行
        run("pnpx markdownlint-cli2 --fix '**/*.md'")

        # リンクチェック実行
        if config_files["link-check"]:
            run("fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json")
    else:
        # 設定ファイル生成を提案
        suggest_config_creation()

        # docs-manager エージェント起動
        run_docs_manager_agent()
```

### 実行例

#### Case 1: mise タスクがある場合

```bash
/fix-docs
→ mise.toml 検出
→ docs:fix タスク検出
→ mise run docs:fix 実行
→ .markdownlint-cli2.jsonc を自動参照
→ 完了
```

#### Case 2: mise タスクがなく、設定ファイルがある場合

```bash
/fix-docs
→ .markdownlint-cli2.jsonc 検出
→ .markdown-link-check.json 検出
→ pnpx markdownlint-cli2 --fix '**/*.md' 実行
→ fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json 実行
→ 完了
```

#### Case 3: 設定ファイルがない場合

```bash
/fix-docs
→ 設定ファイル未検出
→ 設定ファイル生成を提案
→ docs-manager エージェント起動
→ プロジェクト分析と設定最適化
→ 完了
```

## 📖 使用例

### 基本実行

```bash
# 全ドキュメントの分析・修正
/fix-docs

# 特定ディレクトリのみ
/fix-docs docs/

# 設定ファイル生成のみ
/fix-docs --config-only
```

### 高度なオプション

```bash
# リンクのみ修正
/fix-docs --links-only

# フォーマットのみ修正
/fix-docs --format-only

# 構造最適化込み
/fix-docs --optimize-structure

# ドライラン（修正内容の確認のみ）
/fix-docs --dry-run
```

## 🎛️ オプション別動作

### --config-only

設定ファイルの生成・最適化のみ実行します。実際のドキュメント修正は行いません。

```bash
/fix-docs --config-only
```

**実行内容:**

1. 既存設定ファイルの検出
2. 不足している設定の特定
3. テンプレートからの生成
4. プロジェクト特性に応じたカスタマイズ

**生成される設定ファイル:**

- `.markdownlint-cli2.jsonc` - Markdownlint設定
- `.markdown-link-check.json` - リンクチェック設定

**使用例:**

```bash
# 新規プロジェクトでの設定初期化
cd new-project
/fix-docs --config-only

# 出力例
✓ .markdownlint-cli2.jsonc を生成しました
✓ .markdown-link-check.json を生成しました
```

### --links-only

リンクチェックのみ実行します。Markdownフォーマット修正はスキップします。

```bash
/fix-docs --links-only
```

**実行内容:**

- mise タスクがある場合: `mise run docs:links`
- 設定ファイルがある場合: `fd -e md -X pnpx markdown-link-check --config .markdown-link-check.json`

**出力例:**

```text
✓ README.md - すべてのリンクが有効です
✗ docs/api.md - 2個のリンク切れを検出
  - https://old-domain.com/api → 404 Not Found
  - ./missing-file.md → ファイルが存在しません
✓ CHANGELOG.md - すべてのリンクが有効です
```

### --format-only

フォーマット修正のみ実行します。リンクチェックはスキップします。

```bash
/fix-docs --format-only
```

**実行内容:**

- mise タスクがある場合: `mise run docs:fix`
- 設定ファイルがある場合: `pnpx markdownlint-cli2 --fix '**/*.md'`

**修正内容:**

- Markdownシンタックスエラー
- ヘッダー階層の整理
- テーブルフォーマット
- コードブロックのフェンススタイル統一

**出力例:**

```text
✓ README.md - 3箇所を修正
  - MD009: Trailing spaces removed
  - MD047: Added final newline
  - MD046: Code fence style unified
✓ docs/guide.md - 修正なし
✓ CHANGELOG.md - 1箇所を修正
```

### --optimize-structure

構造最適化を含む包括的な修正を実行します。docs-manager エージェントを必ず起動します。

```bash
/fix-docs --optimize-structure
```

**実行内容:**

1. 基本的なフォーマット修正・リンクチェック
2. docs-manager エージェント起動
3. ドキュメント構造の最適化
4. メタデータの検証・修正
5. プロジェクト固有ルールの適用

**最適化内容:**

- 目次（TOC）の自動生成・更新
- セクション順序の論理的整理
- 重複コンテンツの統合
- ドキュメント分割の提案（サイズ超過時）

### --dry-run

修正内容の確認のみで、実際の修正は行いません。

```bash
/fix-docs --dry-run
```

**実行内容:**

1. 問題の検出と報告
2. 修正予定内容の表示
3. 影響範囲の推定

**出力例:**

```text
=== Dry Run モード ===

検出された問題:

リンク問題 (3件):
  - README.md:45 - https://old-domain.com/api
  - docs/api.md:12 - ./missing-file.md
  - docs/guide.md:89 - #invalid-anchor

フォーマット問題 (12件):
  - README.md: MD009 (3箇所), MD047 (1箇所)
  - docs/guide.md: MD013 (5箇所), MD033 (3箇所)

構造問題 (2件):
  - docs/api.md: TOC不整合
  - CHANGELOG.md: セクション順序

修正予定:
  - 15ファイルを分析
  - 17箇所を修正
  - 推定時間: 約30秒

実際に修正を実行するには --dry-run を外してください。
```

## 🤖 docs-manager エージェント統合

内部的にdocs-managerエージェントを呼び出し、以下の高度な機能を実現：

```python
def integrate_docs_manager_agent():
    """docs-managerエージェントとの統合実行"""

    # エージェント実行
    agent_result = Task(
        subagent_type="docs-manager",
        description="ドキュメント包括分析と修正",
        prompt=f"""
        プロジェクトの全ドキュメントを分析し、以下を実行してください：

        1. リンク検証と修正提案
        2. ドキュメント構造の最適化
        3. フォーマット不整合の修正
        4. 重複・矛盾コンテンツの統合
        5. .markdownlint.yml の最適化

        プロジェクト情報:
        - タイプ: {project_info['type']}
        - 技術スタック: {project_info['stack']}

        対象ファイル: {len(target_files)}件
        """
    )

    return process_agent_recommendations(agent_result)
```

## 📊 実行フロー

```text
/fix-docs 実行
    ↓
┌─────────────────┐
│ mise.toml 存在? │
└────┬────────────┘
     │
  Yes│  No
     ↓   ↓
┌─────────────────┐  ┌──────────────────┐
│ docs:fix 定義?  │  │ 設定ファイル存在?│
└────┬────────────┘  └────┬─────────────┘
     │                     │
  Yes│  No              Yes│  No
     ↓   ↓                 ↓   ↓
 [mise実行] [直接実行]  [設定使用] [設定生成提案]
     │      │                │        ↓
     │      │                │  [docs-manager]
     │      │                │        ↓
     └──────┴────────────────┴─→ [完了]
```

### 各ステップの詳細

#### 1. mise.toml チェック

- プロジェクトルートで `mise.toml` の存在を確認
- 存在する場合は `docs:*` タスクを抽出

#### 2. タスク定義チェック

- `docs:fix`, `docs:lint`, `docs:links`, `docs:check` の定義を確認
- タスクが定義されている場合は優先的に使用

#### 3. 設定ファイルチェック

- `.markdownlint-cli2.jsonc` または `.markdownlint.yml`
- `.markdown-link-check.json`
- 設定ファイルの妥当性検証

#### 4. 実行方法決定

- **mise実行**: `mise run docs:fix && mise run docs:links`
- **設定使用**: `pnpx markdownlint-cli2 --fix '**/*.md'` + リンクチェック
- **設定生成**: テンプレートから設定ファイル生成 → docs-manager エージェント起動

#### 5. 結果検証

- 修正内容の確認
- エラー有無のチェック
- 追加修正の必要性判断

## 🎯 品質保証

### 修正前検証

- Git状態の確認（uncommittedな変更の警告）
- バックアップ作成の提案
- 重要ファイル（README等）の慎重な取り扱い

### 修正後検証

- リンク修正の動作確認
- フォーマットの一貫性検証
- 既存コンテンツの保持確認
- エラー修正の完了確認

### 安全性保証

- **コンテンツ保護**: 元のコンテンツ意味を保持
- **段階的修正**: 問題を分類して段階的に対処
- **元に戻す機能**: 問題のある修正の取り消し
- **詳細レポート**: 全修正内容の詳細報告

## 🔄 継続的改善

### 学習機能

- プロジェクト固有の問題パターンを学習
- 修正履歴から最適な手法を推定
- チーム固有のドキュメント規約を学習

### 統合ワークフロー

```bash
# 実装完了後のドキュメント更新フロー
/implement feature && /fix-docs && /review

# リリース前のドキュメント品質確認
/fix-docs --comprehensive && /docs && /commit
```

---

**目標**: プロジェクト全体のドキュメント品質を自動で維持し、開発者がコンテンツ作成に集中できる環境を提供

---

## 🎯 Skill Integration

このコマンドは以下のスキルと統合し、包括的なドキュメント修正を実現します。

### docs-manager (必須)

- **理由**: ドキュメント品質管理のコアスキル
- **タイミング**: `--optimize-structure` または高度な分析が必要な場合に起動
- **トリガー**: `/fix-docs --optimize-structure` または設定ファイル未検出時
- **提供内容**:
  - ドキュメント構造最適化
  - メタデータ検証と修正
  - プロジェクト固有ルールの適用
  - 重複・矛盾コンテンツの統合
  - リンク検証と修正提案

### markdown-docs (オプション)

- **理由**: Markdownドキュメント品質評価と改善
- **タイミング**: ドキュメント品質問題が検出された場合
- **トリガー**: フォーマット問題や構造問題が多数検出された場合
- **提供内容**:
  - 文章品質評価（5段階評価）
  - 文書構造分析
  - 技術的正確性検証
  - 読者体験最適化
  - アクションアイテム提案

### mise (条件付き)

- **理由**: mise.toml タスクによる統一実行
- **タイミング**: mise.toml に `docs:*` タスクが定義されている場合
- **トリガー**: プロジェクトルートに mise.toml が存在する場合
- **提供内容**:
  - `docs:fix` - Markdownlint自動修正
  - `docs:lint` - Markdownlint検証
  - `docs:links` - リンクチェック
  - `docs:check` - 包括的チェック

### 統合フローの例

**Case 1: mise タスク優先フロー**:

```
/fix-docs 実行
    ↓
mise.toml 検出
    ↓
docs:fix タスク検出
    ↓ (mise skill統合)
mise run docs:fix 実行
    ↓ (.markdownlint-cli2.jsonc 自動参照)
Markdownlint自動修正
    ↓
mise run docs:links 実行
    ↓ (.markdown-link-check.json 自動参照)
リンクチェック
    ↓
完了報告
```

**Case 2: 構造最適化フロー（docs-manager統合）**:

```
/fix-docs --optimize-structure 実行
    ↓
基本修正（Markdownlint + リンクチェック）
    ↓
docs-manager skill起動
    ↓
ドキュメント構造分析
    ↓ (メタデータ検証)
最終更新、対象、タグの確認
    ↓ (構造最適化)
目次自動生成・更新
    ↓ (重複統合)
重複コンテンツの統合提案
    ↓
最適化結果報告
```

**Case 3: 設定生成フロー（markdown-docs統合）**:

```
/fix-docs 実行
    ↓
設定ファイル未検出
    ↓
docs-manager skill起動
    ↓
プロジェクト特性分析
    ↓ (markdown-docs統合)
品質評価基準の適用
    ↓
設定ファイル生成:
  - .markdownlint-cli2.jsonc
  - .markdown-link-check.json
    ↓ (プロジェクト特性に応じたカスタマイズ)
API ドキュメント → コードブロック言語指定厳格化
    ↓
設定生成完了報告
```

### オプション別スキル統合

| オプション             | 統合スキル   | 提供機能                               |
| ---------------------- | ------------ | -------------------------------------- |
| `--config-only`        | docs-manager | 設定ファイル生成・最適化               |
| `--links-only`         | なし         | リンクチェックのみ（ツール直接実行）   |
| `--format-only`        | なし         | フォーマット修正のみ（ツール直接実行） |
| `--optimize-structure` | docs-manager | 包括的構造最適化                       |
| `--dry-run`            | docs-manager | 問題検出と修正予定内容の表示           |

### スキル連携の利点

1. **自動検出と最適実行**: mise/設定ファイルの自動検出により最適な実行方法を選択
2. **高度な分析**: docs-managerによる構造最適化とメタデータ検証
3. **品質保証**: markdown-docsによる5段階品質評価
4. **プロジェクト適応**: プロジェクト特性に応じた設定ファイル生成
5. **Progressive Disclosure**: 必要に応じてスキルを段階的にロード

---
