# ========================================
# ~/.config 配下のみで有効にする mise タスク
# プロジェクト固有の集約・mac依存タスクをここで定義
# ========================================

# タスク定義を mise/tasks/ から読み込み
[task_config]
includes = [
  "mise/tasks/format.toml",
  "mise/tasks/lint.toml",
  "mise/tasks/test.toml",
  "mise/tasks/integration.toml",
]

# ========================================
# Agent Skills (skills-internal)
# ========================================

[env]
MARKDOWN_LINK_CONFIG = ".markdown-link-check.json"
AGENTS_MD_FILES = "fd -e md --type f --no-follow . agents/skills-internal"

[tasks."agents:docs:lint"]
description = "agents/skills-internal の Markdown lint"
run = "$AGENTS_MD_FILES -X markdownlint-cli2"

[tasks."agents:docs:fix"]
description = "agents/skills-internal の Markdown lint 自動修正"
run = "$AGENTS_MD_FILES -X markdownlint-cli2 --fix"

[tasks."agents:docs:links"]
description = "agents/skills-internal の Markdown リンクチェック"
run = "$AGENTS_MD_FILES -X markdown-link-check --quiet --config ${MARKDOWN_LINK_CONFIG}"

[tasks."agents:docs:links:verbose"]
description = "agents/skills-internal の Markdown リンクチェック（詳細）"
run = "$AGENTS_MD_FILES -X markdown-link-check --verbose --config ${MARKDOWN_LINK_CONFIG}"

[tasks."agents:docs:format"]
description = "agents/skills-internal の Prettier フォーマット"
run = "$AGENTS_MD_FILES -X prettier --write --log-level warn --ignore-path /dev/null"

[tasks."agents:docs:format:check"]
description = "agents/skills-internal の Prettier フォーマットチェック"
run = "$AGENTS_MD_FILES -X prettier --check --ignore-path /dev/null"

[tasks."skills:install"]
description = "HM でスキルをインストール"
run = "home-manager switch --flake . --impure"

[tasks."skills:list"]
description = "有効スキル一覧を表示"
run = "nix run .#list"

[tasks."skills:validate"]
description = "スキル構成バリデーション"
run = "nix run .#validate"

[tasks."skills:update"]
description = "全ソース更新（flake inputs）"
run = "nix flake update --flake ."

[tasks."skills:fix:bold-headings"]
description = "skills-internal の太字見出しを ### へ変換"
run = "node ./agents/scripts/replace-bold-headings.js"

# ========================================
# Environment Variable Management (dotenvx)
# ========================================

[tasks."setup-env"]
description = ".env を .env.local に復号化（mise hooks から自動実行）"
run = "scripts/setup-env.sh"

[tasks."env:encrypt"]
description = "環境変数ファイルを暗号化"
run = "dotenvx encrypt -f ${XDG_CONFIG_HOME:-$HOME/.config}/.env"

[tasks."env:decrypt"]
description = "環境変数ファイルを復号化"
run = "DOTENV_PRIVATE_KEY_PATH=${XDG_CONFIG_HOME:-$HOME/.config}/.env.keys dotenvx decrypt -f ${XDG_CONFIG_HOME:-$HOME/.config}/.env"

[hooks]
# Decrypt .env to .env.local on directory enter (silent on success, show critical errors)
enter = 'mise run setup-env 2>&1 | grep -E "^(❌|Error:)" || true'

# ========================================
# Brewfile 管理
# ========================================

[tasks."brewfile:backup"]
description = "現在のインストール状況をBrewfileに保存（差分あればコミット）"
run = "if command -v brew >/dev/null 2>&1; then brew bundle dump --file=Brewfile --force && git add Brewfile && git diff --cached --quiet || git commit -m 'chore: update Brewfile'; else echo 'Homebrew not installed'; fi"

[tasks."brewfile:restore"]
description = "Brewfileからパッケージをインストール（新規Mac対応）"
run = "if command -v brew >/dev/null 2>&1; then brew bundle install --no-upgrade; else echo 'Homebrew not installed. Install: /bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\"'; fi"

# ========================================
# 更新タスク（mac/local 依存）
# ========================================
[tasks.update]
description = "すべての依存関係を更新"
depends = [
  "update:submodules",
  "update:brew",
  "update:apt",
  "update:external-repos",
]

[tasks."update:submodules"]
description = "Git サブモジュールを最新に更新（ローカル変更を破棄）"
run = "if [ -f .gitmodules ]; then git submodule foreach 'git reset --hard HEAD && git clean -fd' && git submodule update --remote; else echo 'No submodules configured, skipping'; fi"

[tasks."update:brew"]
description = "Homebrew パッケージを更新（formula のみ、cask は除く）"
run = "if command -v brew >/dev/null 2>&1; then (brew update 2>&1 | grep -v \"definition is invalid\" || true) && brew upgrade --formula; else echo 'Homebrew not installed, skipping brew update'; fi"

[tasks."update:apt"]
description = "APT パッケージを更新（Ubuntu/Debian 環境）"
run = "if command -v apt-get >/dev/null 2>&1 && [ -f /etc/debian_version ]; then sudo apt-get update && sudo apt-get upgrade -y; else echo 'APT not available, skipping apt update'; fi"

[tasks."update:external-repos"]
description = "外部 Git リポジトリを更新（強制リセット）"
run = [
  "if [ -d ~/src/github.com/brookhong/Surfingkeys ]; then cd ~/src/github.com/brookhong/Surfingkeys && git fetch origin && git reset --hard origin/master && git clean -fd; else echo 'Skip: ~/src/github.com/brookhong/Surfingkeys not found'; fi",
]

# ========================================
# Home Manager 管理
# ========================================
[tasks."hm:switch"]
description = "Home Manager を適用（dotfiles配布）"
run = "home-manager switch --flake . --impure"

[tasks."hm:check"]
description = "Home Manager 設定の検証（ビルドのみ、適用なし）"
run = "home-manager build --flake . --impure"

[tasks."hm:generations"]
description = "Home Manager の世代一覧を表示"
run = "home-manager generations"

[tasks."hm:rollback"]
description = "Home Manager を前の世代にロールバック"
run = """
  # List generations and select the previous one
  prev_gen=$(home-manager generations | head -2 | tail -1 | awk '{print $NF}')
  if [ -z "$prev_gen" ]; then
    echo "❌ No previous generation found"
    exit 1
  fi
  echo "📦 Rolling back to generation: $prev_gen"
  home-manager switch --flake . --impure --switch-generation "$prev_gen"
"""

[tasks."hm:clean"]
description = "古い Home Manager 世代を削除（30日以上前）"
run = "home-manager expire-generations '-30 days'"

# CI/CD
[tasks.ci]
description = "すべての CI チェックをローカル実行（GitHub Actions と同じ）"
depends = [
  "format",
  "lint:docs",
  "lint:links",
  "test:lua",
  "agents:docs:lint",
  "agents:docs:format:check",
  "agents:docs:links",
  "skills:validate",
]

[tasks."ci:install"]
description = "CI に必要な追加ツールをインストール"
run = """
luarocks install --local luacheck
luarocks install --local busted
"""

[tasks."ci:deploy"]
description = "CI環境でファイル配布（Home Manager）"
run = "nix run home-manager -- switch --flake . --impure -b backup"
