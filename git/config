[include]
  path = ~/.config/git/config_delta
  path = ~/.config/git/config_ghq
  path = ~/.config/git/config_alias

[color]
  branche = auto
  diff    = auto
  interactive = auto
  status  = auto
  ui = auto

[core]
  excludesfile = ~/.config/git/ignore
  attributesfile = ~/.config/git/attributes
  quotepath = false
  editor = /usr/local/bin/vim

[branch "master"]
  remote = origin
  merge = refs/heads/master

[http]
  sslVerify = false

[push]
  default = simple

[credential]
  helper = cache --timeout=3600

[filter "media"]
  required = true
  clean = git media clean %f
  smudge = git media smudge %f

[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
