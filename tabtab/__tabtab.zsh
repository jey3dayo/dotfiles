
# tabtab source for serverless package
# uninstall by removing these lines
[[ -f ~/.config/tabtab/serverless.zsh ]] && . ~/.config/tabtab/serverless.zsh || true

# tabtab source for sls package
# uninstall by removing these lines
[[ -f ~/.config/tabtab/sls.zsh ]] && . ~/.config/tabtab/sls.zsh || true
